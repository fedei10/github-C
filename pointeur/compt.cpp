#include <stdio.h>
#define n 2 
typedef struct 
{
	hh int ;
	mm int ; 
}instant ;
struct instant instants[n];
void saisie (instant *t )
{
	int c ; 
c=(*t)malloc(n*sizeof(*t));

for (i<1;i<=c;i++){

do {
printft("donner l'heure : '")	
scanf("%d",)	
}while (t.int<=0 && (t.int>=24));

do {
	printf("donner les mm : ");
	scanf("%d",&instants.int);
}
}

#include <stdio.h>
#include <stdiolib.h>

typedef point 
{
	float x , y;
		
}dim;

void get_point ( dim *p) {
  do 
  { printf(" donner x= \n");
  	scanf("%d", &(d->aa));
  	
	  }while ((d->aa)<0 );
	  
  do 
  { printf(" donner y=  \n");
  	scanf("%d", &(p->y);
  	
	  }while (   (d->mm<0)  && (d->mm >12) ) ;	
	
	
}
